---
- name: "Enroll in IPA server"
  hosts: games
  roles:
    - role: freeipa-client
      vars:
        client:
          addresses: "{{ interfaces.games.net0.addresses }}"
  vars_files:
    - "vars/freeipa.yml"
    - "vars/network.yml"
    - "vars/secrets.yml"

- name: Ensure Minecraft Server
  hosts: games
  roles:
    - minecraft
  
