Docker Swarm Create role
=========

Creates a swarm on every host specified, skips if swarm already exists, saves the manager/worker tokens on the host as a fact (swarm_manager_token/swarm_worker_token)