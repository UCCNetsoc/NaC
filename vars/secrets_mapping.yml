proxmox: "{{ vault_proxmox }}"
auditor: "{{ proxmox.auditor }}"

ipa:
  ds_password: "{{ vault_ipa.ds_password }}"
  admin_password: "{{ vault_ipa.admin_password }}"
  domain: "infra.netsoc.co"
  server: "ipa.infra.netsoc.co"
  realm: "INFRA.NETSOC.CO"

keycloak: "{{ vault_keycloak }}"

docker_hub: "{{ vault_docker_hub }}"

mysql: "{{ vault_mysql }}"
postgres: "{{ vault_postgres }}"

traefik_basic_auth: "{{ vault_traefik_basic_auth }}"
traefik: "{{ vault_traefik }}"

sendgrid: "{{ vault_sendgrid }}"

netsocbot: '{{ vault_netsocbot }}'

wikijs: "{{ vault_wikijs }}"

prometheus: "{{ vault_prometheus }}"

ldap: "{{ vault_ldap }}"

cloudflare: "{{ vault_cloudflare }}"

grafana: "{{ vault_grafana }}"
grafana_admin_pass: "{{ vault_grafana_admin_pass }}"

drone: "{{ vault_drone }}"

portainer: "{{ vault_portainer }}"

webhooks: "{{ vault_webhooks }}"

provisioning_root_password: "{{ vault_provisioning_root_password }}"
packer: "{{ vault_packer }}"

github: "{{ vault_github }}"

gdrive: '{{ vault_gdrive }}'
#   user_id
#   client_id
#   client_secret
#   cache
#   location

loki: "{{ vault_loki }}"

shortener: "{{ vault_shortener }}"