freeipa:
  ds_password: "{{ vault_freeipa.ds_password }}"
  admin_password: "{{ vault_freeipa.admin_password }}"
  domain: "vm.netsoc.co"
  server: "ipa.vm.netsoc.co"
  realm: "VM.NETSOC.CO"

# Used for ipa_host module
ipa_host: "ipa.vm.netsoc.co"
ipa_pass: "{{ vault_freeipa.admin_password }}"
